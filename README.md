# Умскул — Telegram Mini App для квалификации клиента

Мини-апп для сегментации входящего трафика онлайн-школы: за 5 коротких вопросов собирает
профиль лида и отдаёт его в бота структурированным JSON.

## Вопросы

| # | Вопрос | Формат |
|---|--------|--------|
| 1 | Ученик или родитель | одиночный выбор |
| 2 | В какой класс переходит | 5–11 класс |
| 3 | Какие предметы будет сдавать | мультивыбор |
| 4 | Желаемый результат | баллы (ЕГЭ) или оценка (ОГЭ / школа) |
| 5 | Как готовится сейчас | никак / сам / репетитор / уже в Умскул |

Набор предметов и формат цели зависят от класса: 9 класс → ОГЭ (оценка 3–5),
10–11 → ЕГЭ (ползунок 40–100 баллов), 5–8 → школьная программа (оценка 3–5).
Формулировки меняются в зависимости от роли: ученику — «ты», родителю — «ребёнок».

## Что внутри

- Прогресс-бар: процент, «Вопрос N из 5» и посегментные индикаторы шагов.
- Анимации: переходы шагов влево/вправо, каскадное появление карточек, pop-эффект
  на выборе, «рисование» галочки, конфетти на финальном экране.
- Мобильная адаптация: mobile-first, `100dvh`, safe-area, брейкпоинты на 320 и 480px,
  учёт `prefers-reduced-motion`.
- Интеграция с Telegram: `MainButton` и `BackButton`, тактильный отклик,
  светлая/тёмная тема из клиента, `expand()`, подтверждение закрытия.
- Ответы сохраняются в `localStorage` — перезагрузка не теряет прогресс.

Вне Telegram (обычный браузер) мини-апп полностью работоспособен: показывается
собственная кнопка внизу, а на финале ответы копируются в буфер обмена.

## Стек

Статика без сборки: `index.html` + `assets/styles.css` + `assets/app.js`.
Никаких зависимостей и npm-шага — файлы можно положить на любой HTTPS-хостинг.

```
index.html            разметка оболочки: топбар, прогресс, экран, футер
assets/styles.css     дизайн-система Умскул, тёмная тема, анимации
assets/app.js         состояние, шаги, навигация, интеграция с Telegram
bot/bot.py            пример приёма ответов на стороне бота (aiogram 3)
```

## Локальный запуск

```bash
python3 -m http.server 4173
# открыть http://localhost:4173
```

Чтобы проверить внутри Telegram, нужен HTTPS-URL — подойдёт туннель
(`cloudflared tunnel --url http://localhost:4173`) или GitHub Pages.

## Публикация на GitHub Pages

В репозитории есть workflow `.github/workflows/pages.yml`: включите Pages в
`Settings → Pages → Build and deployment → GitHub Actions`, после пуша в основную ветку
мини-апп будет доступен по `https://<user>.github.io/<repo>/`.

## Подключение к боту

Мини-апп возвращает ответы через `Telegram.WebApp.sendData()`. Это работает только
если приложение открыто кнопкой **reply-клавиатуры** (`KeyboardButton` с `web_app`).
При запуске из меню бота или инлайн-кнопки `sendData` недоступен — там нужно слать
ответы на свой бэкенд с проверкой `initData`.

Пример payload:

```json
{
  "v": 1,
  "source": "umskul_qualification",
  "role": "parent",
  "grade": 10,
  "exam": "ege",
  "goal_unit": "score",
  "subjects": [
    { "id": "soc", "name": "Обществознание", "goal": 90 },
    { "id": "hist", "name": "История", "goal": 90 }
  ],
  "goal_avg": 90,
  "preparation": "umschool",
  "recommendation": "Рекомендуем интенсив на 90+",
  "ts": "2026-07-27T22:10:29.458Z"
}
```

Значения полей: `role` — `student` | `parent`; `exam` — `ege` | `oge` | `school`;
`goal_unit` — `score` (баллы ЕГЭ) | `mark` (оценка 3–5);
`preparation` — `none` | `self` | `tutor` | `umschool`.

Приём на стороне бота — `bot/bot.py`:

```bash
pip install aiogram
BOT_TOKEN=123:ABC WEBAPP_URL=https://<user>.github.io/<repo>/ python bot/bot.py
```

## Кастомизация

- Палитра и радиусы — переменные в `:root` в `assets/styles.css`
  (`--brand`, `--accent`, `--radius`).
- Списки предметов, вариантов подготовки и классов — константы `SUBJECTS`, `PREP`,
  `GRADES` в начале `assets/app.js`.
- Логика рекомендации на финальном экране — функции `recommendation()` и `planItems()`.
