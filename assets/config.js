/* ==========================================================================
   Настройки интеграции мини-аппа.
   Правится без пересборки — обычный файл, который грузится перед app.js.
   ========================================================================== */
window.UMSKUL_CONFIG = {
  /**
   * Куда мини-апп отправляет ответы по HTTP (POST, JSON).
   * Это основной способ: он работает при любом способе запуска мини-аппа,
   * включая прямую ссылку t.me/<bot>/<app> из сообщения бота.
   *
   * Пустая строка — HTTP-отправка выключена; тогда мини-апп пробует
   * Telegram.WebApp.sendData(), который работает только если приложение
   * открыто кнопкой reply-клавиатуры.
   *
   * Пример: 'https://api.example.com/umskul/quiz'
   */
  submitUrl: '',

  /** Таймаут отправки, мс. */
  submitTimeout: 15000,

  /**
   * Произвольные поля, которые уедут вместе с ответами
   * (например, идентификатор воронки или источника трафика).
   */
  extra: {}
};
